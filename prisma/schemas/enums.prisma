enum UserRole {
     CUSTOMER
     PROVIDER
     ADMIN
}

enum UserStatus {
     ACTIVE
     SUSPENDED
}

enum OrderStatus {
     PLACED
     CONFIRMED
     PAID
     PICKED_UP
     RETURNED
     CANCELLED
}

enum PaymentMethod {
     SSLCOMMERZ
}

enum PaymentStatus {
     PENDING
     PAID
     COMPLETED
     FAILED
     REFUNDED
}
